<div>
<div class="pull-right">
</div>
    <div class="well">
        {{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}
        
        {# Les erreurs générales du formulaire #}
        {{ form_errors(form) }}

        {# Génération manuelle du formulaire #}
        {# Nom de la cible #}
        <div class="form-group my-5">
            {# Génération du label #}
            {{ form_label(form.nom, "Nom de la cible", {'label_attr': {'class': 'cal-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis #}
            {{ form_errors(form.nom) }}
            <div>
                {# Génération de l'input. #}
                {{ form_widget(form.nom, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
        {# Prénom de la cible #}
        <div class="form-group my-5">
            {# Génération du label #}
            {{ form_label(form.prenom, "Prénom de la cible", {'label_attr': {'class': 'cal-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis #}
            {{ form_errors(form.prenom) }}
            <div>
                {# Génération de l'input. #}
                {{ form_widget(form.prenom, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
        {# Date de Naissance de la cible #}
        <div class="form-group my-5">
            {# Génération du label #}
            {{ form_label(form.DateNaissance, "Date de naissance de la cible", {'label_attr': {'class': 'cal-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis #}
            {{ form_errors(form.DateNaissance) }}
            <div>
                {# Génération de l'input. #}
                {{ form_widget(form.DateNaissance, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
        {# Nom de code de la cible #}
        <div class="form-group my-5">
            {# Génération du label #}
            {{ form_label(form.nomCode, "Nom de code de la cible", {'label_attr': {'class': 'cal-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis #}
            {{ form_errors(form.nomCode) }}
            <div>
                {# Génération de l'input. #}
                {{ form_widget(form.nomCode, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
        {# Mission de la cible #}
        <div class="form-group my-5">
            {# Génération du label #}
            {{ form_label(form.mission, "Missions de la cible", {'label_attr': {'class': 'cal-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis #}
            {{ form_errors(form.mission) }}
            <div>
                {# Génération de l'input. #}
                {{ form_widget(form.mission, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
        {# Nationnalité de la cible #}
        <div class="form-group my-5">
            {# Génération du label #}
            {{ form_label(form.nationnalite, "Nationnalité de la cible ", {'label_attr': {'class': 'cal-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis #}
            {{ form_errors(form.nationnalite) }}
            <div>
                {# Génération de l'input. #}
                {{ form_widget(form.nationnalite, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
        <p>
        {# Génération automatique des champs pas encore écrits #}
        {{ form_rest(form) }}
        {# Pour le bouton, pas de label ni d'erreur, on affiche juste le widget #}
        {{ form_widget(form.Sauvegarde, {'attr': {'class': 'btn btn-primary'}}) }}
        {# Fermeture de la balise <form> de formulaire HTML #}
        {{ form_end(form) }}
    </div>
</html>
