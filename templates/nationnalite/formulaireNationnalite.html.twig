<div>
<div class="pull-right">
</div>
    <div class="well">
        {{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}
        
        {# Les erreurs générales du formulaire #}
        {{ form_errors(form) }}

        {# Génération manuelle du formulaire #}
        {# Nom de la nationnalité #}
        <div class="form-group my-5">
            {# Génération du label #}
            {{ form_label(form.nomNatio, "Nom de la nationnalité", {'label_attr': {'class': 'cal-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis #}
            {{ form_errors(form.nomNatio) }}
            <div>
                {# Génération de l'input. #}
                {{ form_widget(form.nomNatio, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
        {# Pays de la nationnalité #}
        <div class="form-group my-5">
            {# Génération du label #}
            {{ form_label(form.Pays, "Pays", {'label_attr': {'class': 'cal-sm-2 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis #}
            {{ form_errors(form.Pays) }}
            <div>
                {# Génération de l'input. #}
                {{ form_widget(form.Pays, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
        <p>
        {# Génération automatique des champs pas encore écrits #}
        {{ form_rest(form) }}
        {# Pour le bouton, pas de label ni d'erreur, on affiche juste le widget #}
        {{ form_widget(form.Sauvegarde, {'attr': {'class': 'btn btn-primary'}}) }}
        {# Fermeture de la balise <form> de formulaire HTML #}
        {{ form_end(form) }}
    </div>
</html>
